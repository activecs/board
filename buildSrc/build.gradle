apply plugin: 'groovy'
apply plugin: 'ssh'

repositories {
	mavenCentral()
}

dependencies {
	compile 'org.yaml:snakeyaml:1.13'
}

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'org.hidetake:gradle-ssh-plugin:0.3.10'
    }
}